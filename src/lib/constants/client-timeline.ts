/** Máximo de linhas buscadas por fonte (audit / transições) antes do merge. */
export const CLIENT_TIMELINE_FETCH_CAP = 320;

/** Máximo de itens após merge antes da paginação na janela. */
export const CLIENT_TIMELINE_MERGE_CAP = 400;
