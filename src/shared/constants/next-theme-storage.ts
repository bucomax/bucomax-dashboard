/**
 * Chave do `next-themes` no `localStorage`. Deve coincidir com `storageKey` do
 * `ThemeProvider` e com a string em `theme-blocking-script.tsx`.
 */
export const NEXT_THEME_LOCAL_STORAGE_KEY = "next-theme";
