services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: idoctor
      POSTGRES_PASSWORD: idoctor
      POSTGRES_DB: idoctor
    ports:
      - "5432:5432"
    volumes:
      - idoctor_pg:/var/lib/postgresql/data

volumes:
  idoctor_pg:
